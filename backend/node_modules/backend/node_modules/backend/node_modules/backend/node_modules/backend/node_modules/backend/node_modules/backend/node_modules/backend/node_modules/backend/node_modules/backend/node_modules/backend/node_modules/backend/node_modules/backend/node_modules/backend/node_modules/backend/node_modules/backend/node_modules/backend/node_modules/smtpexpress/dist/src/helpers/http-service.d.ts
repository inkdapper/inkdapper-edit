import { AxiosResponse } from "axios";
import { HttpClientConstructorParams, HttpClientParams, HttpClientResolverError } from "./types";
export declare class HttpService {
    private axiosInstance;
    constructor(params: HttpClientConstructorParams);
    resolver<T>(fn: Promise<AxiosResponse>): Promise<{
        data: T;
        error: HttpClientResolverError;
    }>;
    SendRequest<DAO, DTO = Record<any, any>, DQO = Record<any, any>>(params: HttpClientParams<DTO, DQO>): Promise<{
        data: DAO;
        error: HttpClientResolverError;
    }>;
}
//# sourceMappingURL=http-service.d.ts.map