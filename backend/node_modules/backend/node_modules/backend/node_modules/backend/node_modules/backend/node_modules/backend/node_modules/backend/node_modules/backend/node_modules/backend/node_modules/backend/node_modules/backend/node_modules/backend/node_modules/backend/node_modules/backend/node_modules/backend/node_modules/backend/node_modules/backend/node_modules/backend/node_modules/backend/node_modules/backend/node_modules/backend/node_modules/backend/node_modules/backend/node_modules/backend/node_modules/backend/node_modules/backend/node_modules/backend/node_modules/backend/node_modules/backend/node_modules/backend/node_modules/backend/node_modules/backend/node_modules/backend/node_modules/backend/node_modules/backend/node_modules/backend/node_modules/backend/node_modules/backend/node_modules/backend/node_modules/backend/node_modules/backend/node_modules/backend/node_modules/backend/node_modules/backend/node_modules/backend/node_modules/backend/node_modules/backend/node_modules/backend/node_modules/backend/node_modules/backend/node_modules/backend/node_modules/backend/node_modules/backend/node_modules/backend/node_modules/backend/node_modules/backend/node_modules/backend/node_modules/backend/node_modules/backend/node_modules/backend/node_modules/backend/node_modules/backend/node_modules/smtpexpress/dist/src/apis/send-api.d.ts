import { HttpService } from "../helpers/http-service";
import { SendMailOptions } from "../helpers/types";
export declare function sendAPI(httpService: HttpService): {
    sendMail(options: SendMailOptions): Promise<{
        message: string;
        statusCode: number;
        data: {
            ref: string;
        };
    }>;
};
//# sourceMappingURL=send-api.d.ts.map