import { CredentialOptions } from "./types";
export declare function createClient(credentials: CredentialOptions): {
    sendApi: {
        sendMail(options: import("./types").SendMailOptions): Promise<{
            message: string;
            statusCode: number;
            data: {
                ref: string;
            };
        }>;
    };
};
//# sourceMappingURL=client.d.ts.map